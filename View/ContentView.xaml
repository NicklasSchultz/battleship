<UserControl x:Class="Battleship.View.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:content="clr-namespace:Battleship.Model"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type content:Hit}">
            
        </DataTemplate>
        <DataTemplate DataType="{x:Type content:Ship}" >
            <Viewbox>
                <Rectangle Fill="LightGray"  Height="30" Width="30"></Rectangle>
            </Viewbox>
        </DataTemplate>

        <DataTemplate DataType="{x:Type content:Water}" >
            <Viewbox>
                <Rectangle Fill="LightBlue" Stroke="Black"  StrokeThickness="0.5" Height="30" Width="30"/>
            </Viewbox>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{Binding Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
