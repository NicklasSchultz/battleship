<UserControl x:Class="Battleship.View.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:content="clr-namespace:Battleship.Model"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type content:Hit}">
            
        </DataTemplate>
        <DataTemplate DataType="{x:Type content:Ship}" >
            <Viewbox>
                <Canvas Height="80" Width="80">
                    <Path Stroke="Black" StrokeThickness="2" Stretch="Fill" Fill="Black" Data="M80,89 L20,89 Q20,69 43,55 C30,48 30,35 43,28 A11,11,0,1,1,57,28 C70,35 70,48 57,55 Q80,69 80,89 z" Height="70" Width="70" Margin="5,10,0,0"></Path>
                </Canvas>
            </Viewbox>
        </DataTemplate>

        <DataTemplate DataType="{x:Type content:Water}" >
            <Viewbox>
                <Rectangle Fill="LightBlue" Stroke="Black" Height="30" Width="30"/>
            </Viewbox>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <ContentControl Content="{Binding Mode=OneWay}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
