<UserControl x:Class="Battleship.View.GameMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="1"  Width="200">
            <Button x:Name="nextPlayer" DockPanel.Dock="Top" Content="Next Player" Height="100"/>
            <Button x:Name="exitGame" DockPanel.Dock="Bottom" Content="Exit" Height="100"/>
            <Button x:Name="saveGame" DockPanel.Dock="Bottom" Content="Save" Height="100"/>
        </DockPanel>
    </Grid>
</UserControl>
