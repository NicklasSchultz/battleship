<UserControl x:Class="Battleship.View.CellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:types="clr-namespace:Battleship.View.Ships"
             xmlns:C="clr-namespace:Battleship.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.InputBindings>
            <MouseBinding Command="{Binding Path=MakeSelected}" Gesture="LeftClick"></MouseBinding>
        </Grid.InputBindings>
        <Border BorderThickness="0.5" BorderBrush="Black"
                Width="30"
                Height="30">
            <C:ContentView DataContext="{Binding Path=Content}"></C:ContentView>
        </Border>
    </Grid>
</UserControl>
