<UserControl x:Class="Battleship.View.MainMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ViewModel="clr-namespace:Battleship.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <ViewModel:MainMenuViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Content="Start new game" Width="auto" Height="auto" Command="{Binding ViewChangedCommand}" CommandTarget=""/>
        <Button Grid.Row="1" Content="Load game" Width="auto" Height="auto" Command="{Binding ViewChangedCommand}"/>
        <Button Grid.Row="2" Content="Exit" Width="auto" Height="auto" Command="{Binding ViewChangedCommand}"/>
    </Grid>
</UserControl>
